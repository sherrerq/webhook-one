# Backend config para el ejemplo demo del pipeline de lab-15.
# El bucket se pasa via -backend-config="bucket=..." en terraform init.
region       = "us-east-1"
key          = "lab15-pipeline-demo/terraform.tfstate"
encrypt      = true
use_lockfile = true